# SQLite Search Index

## 役割

Markdownが唯一の正本です。`.totonoe/index.sqlite` は検索候補を高速に絞り込むための派生データであり、削除・破損しても全Entryから再構築できます。

ワークスペース内の保存先ではワークスペース直下、明示的に選択した外部リポジトリでは選択フォルダー直下に `.totonoe/index.sqlite`を作成します。

## 同期

検索の直前に、保存先にあるMarkdownの相対パス、更新時刻、サイズをインデックスと比較します。

- 新規・変更ファイルだけを読み、検索トークンを更新する
- 削除されたファイルの行を除去する
- 変更のないファイル本文は読まない
- DBがない、壊れている、またはスキーマ版が異なる場合は新規作成する

全件再構築は `Totonoe Knowledge: Rebuild Search Index` から実行できます。書き込みは一時ファイルを作成してから置き換えます。

## 検索方式

SQLite FTS3は候補抽出だけに使います。日本語と部分一致を維持するため、正規化したタイトル、要約、キーワード、本文から1〜3文字のn-gramを作成します。

空白なしの4文字以上の日本語検索文は2-gramと3-gramへ展開します。1文字だけの助詞と、助詞・補助表現だけからなる低情報2/3-gramは検索根拠にしません。3-gramが1個以上、または2-gramが2個以上一致したEntryを結果候補とし、英数字は従来どおり正規化した語や部分一致を使用します。SQLiteから読む候補pathは最大200件に制限します。除外規則は候補集合へ依存しないため、SQLite検索と直接scanで同じ検索単位を使用します。

候補Markdownだけを読み、タイトル、要約、キーワード、本文の重みと、一致した検索部分列の長さ・種類を組み合わせて最終順位を決めます。

```text
検索文の正規化・日本語2/3-gram展開
        ↓
SQLite FTS候補抽出（最大200件）
        ↓
低情報部分列の除外 + タイトル 10 / 要約 6 / キーワード 4 / 本文 1
        ↓
Quick PickまたはLanguage Model Toolへ返却
```

SQLiteを利用できない場合は全Markdownの直接検索へフォールバックします。

対象バージョン検索では、検索語に一致しないEntryも含めて`supersedes`関係を評価する必要があるため、現時点では全Markdownを直接読みます。

## 保存内容と機密性

インデックスにはMarkdown原文を保存しません。ただし、相対パスと検索用n-gramは元の語句を推測できる派生情報です。元ナレッジと同じアクセス制御を適用し、GitやCI artifactへ含めないでください。

## 現在の制約

- ファイル変更判定は更新時刻とサイズを使用する
- インデックス全体はsql.jsによって処理時にメモリへ読み込まれる
- 意味検索とEmbeddingはまだ行わない。文字列が重ならない純粋な言い換えや、複数Entryへ同程度に一致する曖昧な文は対象外
- スコア理由の詳細表示は今後のハイブリッド検索で追加する
